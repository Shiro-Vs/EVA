name: CD

on:
  push:
    branches: [ "main", "master", "Probar-Theme2" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Web App
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Simulando Exportacion Expo Web
        run: echo "Exportando bundle de produccion..."

  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Simulando Despliegue en Servidor
        run: echo "Desplegado exitosamente en GitHub Pages."